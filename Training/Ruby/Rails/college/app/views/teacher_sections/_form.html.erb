<%= form_with(model: teacher_section, local: true) do |form| %>
  <% if teacher_section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(teacher_section.errors.count, "error") %> prohibited this teacher_section from being saved:</h2>

      <ul>
      <% teacher_section.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :teacher_id %>
    <%= form.text_field :teacher_id %>
  </div>

  <div class="field">
    <%= form.label :section_id %>
    <%= form.text_field :section_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
